import { FiltroActividadPipe } from './filtro-actividad.pipe';

describe('FiltroActividadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroActividadPipe();
    expect(pipe).toBeTruthy();
  });
});
